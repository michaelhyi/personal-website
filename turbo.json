{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["NODE_ENV", "ORIGIN", "API_URL"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "ci": {},
    "clean": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "format": {},
    "lint": {},
    "prepare": {},
    "setup": {},
    "test": {}
  }
}
