{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["NODE_ENV"],
  "pipeline": {
    "build": {
      "dependsOn": ["next-api#gen", "^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "ci": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "format": {},
    "lint": {},
    "next-api#gen": {},
    "prepare": {},
    "test": {}
  }
}
