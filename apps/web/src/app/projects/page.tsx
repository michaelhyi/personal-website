import Container from "@/components/container";
import ProjectsClient from "@/components/home/projects";

const Projects = () => {
  return (
    <Container>
      <ProjectsClient extended />
    </Container>
  );
};

export default Projects;
