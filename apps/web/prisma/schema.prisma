generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model Post {
  id          Int      @id @default(autoincrement())
  date        DateTime @default(now())
  title       String
  rating      Int
  image       String
  description String
  body        String
}
