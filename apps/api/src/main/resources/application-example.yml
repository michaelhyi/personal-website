spring:
  application:
    name:
  datasource:
    url:
    password:
    username:
  jpa:
    hibernate:
      ddl-auto:
    properties:
      hibernate:
        dialect:
        format_sql:
      show-sql:

auth:
  whitelisted-emails:

aws:
  region:
  s3:
    buckets:
      blog:

security:
  jwt:
    secret-key:
    expiration:
  cors:
    allowed-origins:
    allowed-methods:
    allowed-headers:
    exposed-headers:
