{
   "name": "personal-website",
   "version": "4.1.0",
   "scripts": {
      "ci": "rm -rf node_modules/ && bun install && cd api && mvn clean && cd ../web/ && rm -rf node_modules/ && bun install",
      "format": "prettier --write . && eslint --fix",
      "lint": "cd api && mvn validate && cd ../ && prettier --check . && cd web && bun lint",
      "build": "cd api && mvn package && cd ../web/ && bun run build",
      "prepare": "husky install"
   },
   "keywords": [],
   "author": "michaelyi",
   "license": "MIT",
   "devDependencies": {
      "@types/node": "^20.8.2",
      "eslint": "^8.52.0",
      "husky": "^8.0.3",
      "prettier": "^3.0.3",
      "prettier-plugin-organize-imports": "^3.2.3",
      "prettier-plugin-tailwindcss": "^0.5.6",
      "ts-node": "^10.9.1",
      "typescript": "^5.2.2"
   }
}
